# TireForge.ApiProxy

HTTP API for the dashboard. Azure Functions (isolated worker, .NET 8).

Endpoints: `/status` · `/queue` · `/workorders` · `/cost` · `/simulate` · `/decision`.

References `TireForge.Core`, `TireForge.Data`.
