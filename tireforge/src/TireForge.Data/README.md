# TireForge.Data

**Data** layer.

- Work Order Adapter — interface + implementations for the downstream CMMS
- SQLite store (local dev / demo)
- Seed data

References `TireForge.Core`. Covered by `tests/TireForge.Data.Tests`.
