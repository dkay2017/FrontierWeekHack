# TireForge docs

Design and reference documentation for the TireForge build. Kept inside the
project (not the repo's top-level `docs/`, which is the workshop's MkDocs site)
so it is committed and pushed with the code.

- `design/` — the independent design docs (architecture, technical diagram,
  dashboard UI, TDD) that drive the implementation.
