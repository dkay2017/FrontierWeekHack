<Project>
  <!-- Zynara.Data references Newtonsoft.Json explicitly (the Cosmos SDK 3.x needs
       it at runtime even with the STJ serializer), so the SDK's build check passes
       everywhere it flows transitively — no need to disable it. -->
</Project>
