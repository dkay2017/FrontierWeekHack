{
  "name": "zynara-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Care Approval IQ reviewer console — a single static index.html, no build step.",
  "scripts": {
    "build": "echo \"no build — static index.html\""
  }
}
